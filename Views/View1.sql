-- Pepeu

-- inner join
-- idea: see all products that use some ingredient
SELECT
    -- columns
FROM
    -- tables
INNER JOIN 
    -- table
ON -- condition
;

-- outer join
SELECT
    -- columns
FROM
    -- tables
LEFT JOIN 
    -- other tables
ON 
    -- condition
;

-- group by
-- idea: group by sex, or if the plate is avaliable
SELECT
    -- columns
FROM 
    -- tables
WHERE 
    -- condition
GROUP BY
    -- group
;
